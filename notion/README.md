# notion/

## Status: Reserved, not started

This folder is reserved for a potential Notion workspace export/sync integration, referenced as a planned deliverable in an earlier version of the project roadmap (README.md, pre-2026-08-14).

As of this writing:

- No Notion workspace has been created or linked.
- No sync, export, or integration code exists.
- This item is **not** part of the locked MVP scope (see `docs/MVP.md`).

If this integration is still desired, it should be added to `docs/ROADMAP.md` under an explicit future phase before work begins, per the Constitution's roadmap governance rules. If it is no longer desired, this folder should be removed in a future cleanup pass.
